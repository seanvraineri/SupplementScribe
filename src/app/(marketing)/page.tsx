import AppleLandingPage from './AppleLandingPage';

export default function MarketingPage() {
  // Render the landing page component directly
  return <AppleLandingPage />;
}

// Remove edge runtime as it might conflict with component dependencies
// export const runtime = 'edge';
